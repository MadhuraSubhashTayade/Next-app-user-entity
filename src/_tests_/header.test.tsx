import { Header } from "@/app/components/Header/header";
import { render } from "@testing-library/react";

describe("Header", () => {
  it("renders", () => {
    const {} = render(<Header />);
  });
});
