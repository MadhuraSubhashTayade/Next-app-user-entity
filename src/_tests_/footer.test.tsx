import { Footer } from "@/app/components/Footer/footer";
import { render } from "@testing-library/react";

describe("Footer", () => {
  it("renders", () => {
    const {} = render(<Footer />);
  });
});
